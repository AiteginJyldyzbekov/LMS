import React, { FC } from 'react';
import StudentHeader from '../../studentHeader/studentHeader';
import RoutesStudy from '../Routes/RoutesStudy';

const Courses: FC = () => (
  <div>
    <StudentHeader />
    <RoutesStudy />
  </div>
);

export default Courses;
