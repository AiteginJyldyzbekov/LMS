import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  onBoarding: {
    gridArea: 'o',
  },
  wrapper: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    gap: '40px',
  },
}));
